# For self update:
program_repository = git@gitlab.webtown.hu:webtown/webtown-workflow.git
# For proxy port. After change you have to run: 'wf --init-reverse-proxy' command!
reverse_proxy_port = 80
# Default DNS local proxy TLD:
default_local_tld = .loc

# Directory structure
working_directory = .wf
configuration_file = .wf.yml

# Symfony
symfony_env = prod
xdebug_enabled = 0
