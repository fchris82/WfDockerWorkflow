# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices/configuration.html#application-related-configuration
parameters:
    # Docker path! We load to this path in the `docker-compose.yml` !!!
    program_configuration_file: /etc/webtown-workflow/config
    skeleton_base_dir: '%kernel.root_dir%/../../skeletons'
    recipe_base_dir: '%kernel.root_dir%/../../recipes'

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
        public: false       # Allows optimizing the container by removing unused services; this also means
                            # fetching services directly from the container via $container->get() won't work.
                            # The best practice is to be explicit about your dependencies anyway.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/*'
        exclude: '../src/{Entity,Migrations,Tests,Kernel.php}'

    Recipes\:
        resource: '%recipe_base_dir%/*'
        public: true

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones

    App\Configuration\Builder:
        public: true

    App\Configuration\Configuration:
        public: true

    App\Configuration\RecipeManager:
        arguments:
            - '%recipe_base_dir%'
        calls:
            - method: setContainer
              arguments:
                - '@service_container'
        public: true

    App\Wizard\Manager:
        public: true

#    App\Wizard\Symfony\SymfonyProjectChainWizard:
#        public: true
#        tags:
#            - { name: wizard, group: 'Full builder wizards', priority: 100 }
#
#    App\Wizard\Base\GitCloneWizard:
#        arguments:
#            - '@filesystem'
#            - 'git@github.com:fchris82/symfony-demo.git'
#        tags:
#            - { name: wizard, group: 'Only builders', priority: 50 }
#        public: true
#
#    App\Wizard\Symfony\SymfonyBuildWizard:
#        tags:
#            - { name: wizard, group: 'Only builders', priority: 50 }
#        public: true

    App\Wizard\Symfony\SymfonyComposerBuildWizard:
        tags:
            - { name: wizard, group: 'Builders', priority: 50 }
        public: true

    App\Wizard\Base\GitlabCISkeleton:
        arguments:
            - '%skeleton_base_dir%'
            - '@twig'
            - '@filesystem'
        tags:
            - { name: wizard, group: 'Decorators' }
        public: true

    App\Wizard\Base\PhpCsFixSkeleton:
        arguments:
            - '%skeleton_base_dir%'
            - '@twig'
            - '@filesystem'
        tags:
            - { name: wizard, group: 'Decorators' }
        public: true

    App\Wizard\Base\PhpMdSkeleton:
        arguments:
            - '%skeleton_base_dir%'
            - '@twig'
            - '@filesystem'
        tags:
            - { name: wizard, group: 'Decorators' }
        public: true

    App\Wizard\Base\DeployerSkeleton:
        arguments:
            - '%skeleton_base_dir%'
            - '@twig'
            - '@filesystem'
        tags:
            - { name: wizard, group: 'Decorators', priority: -10 }
        public: true

    App\Wizard\Docker\DevEnvironment:
        arguments:
            - '%skeleton_base_dir%'
            - '@twig'
            - '@filesystem'
        tags:
            - { name: wizard, group: 'Decorators', priority: 20 }
        public: true

    App\Wizard\Docker\DockerProject:
        arguments:
            - '%skeleton_base_dir%'
            - '@twig'
            - '@filesystem'
        tags:
            - { name: wizard, group: 'Decorators', priority: 10 }
        public: true

    App\Wizard\Ez\EzBuildWizard:
        tags:
            - { name: wizard, group: 'Builders', priority: 20 }
        public: true
